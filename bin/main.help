#!/usr/bin/env bash

if [[ "$1" == "description" ]]; then

  echo "运维命令行工具"

elif [[ "$1" == "sub_commands" ]]; then

  # shellcheck disable=SC2005
  echo "$(output_sub_commands)"

elif [[ "$1" == "flags" ]]; then

  echo "
  -h, --help    显示帮助信息
"

fi
